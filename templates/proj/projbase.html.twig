<!DOCTYPE html>
<html lang="sv">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width,initial-scale=1">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="{{ asset('img/favicon.ico') }}">
        {% block javascripts %}
            {% block importmap %}{{ importmap('project') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
        <header>
            <a href="{{ path('proj') }}"><h1 class="title">Poker Squares!</h1></a>
            <nav>
                    <a class="button" href="{{ path('proj') }}">Play!</a>
                    <a class="button hideable" href="{{ path('highscore') }}">Highscores!</a>
                    <a class="button" id="smallscore" href="{{ path('highscore') }}">Scores!</a>
                    <a class="button" href="{{ path('aboutProj') }}">About!</a>
                    <a class="button" href="{{ path('projApi') }}">API!</a>
            </nav>
    </header>
    <main>
        {% block article %}
        <article>
        {% block body %}
        {% endblock body %}
        </article>
        {% endblock article %}
    </main>
    <footer>
        <p> &copy; Challe_P Megaindustries Inc.</p>
        <a class="button" href="{{ path('musicplayer') }}" target="_blank">Some music?</a>    
    </footer>
    </body>
</html>